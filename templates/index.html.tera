<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://js.hcaptcha.com/1/api.js" async defer></script>
    <title>{{ site.title }}</title>
    {{ global | js_global }}
    {{ compactive_js() | wrap_script }}
    <style>
        p{margin:0;}
        .question{
            display: flex;
            gap: 12px;
            flex-direction: column;
        }
        body{
            counter-set: question 0;
        }
        .question-title::before{
            counter-increment: question;
            content: "Q" counter(question) ". ";
            font-weight: bold;
            font-size: 1.35rem;
        }
        .question-title{
            display: flex;
            align-items: center;
            font-weight: normal;
            font-size: 1.3rem;
            gap: 0.4rem;
        }
        .question-description{
            font-weight: 300;
            font-size: 1.2rem;
        }
        .question-captcha{
            display: flex;
            justify-content: center;
        }
        .divider {
            background-color: rgba(0, 0, 0, 0.2);
            height: 1px;
            margin: 16px 0px;
        }
        .question-input{
            margin: 0 4px;
        }
        .question-holder{
            padding: 0 12px;
        }
        input[type="text"] {
            font-size: 1.1rem;
            padding: 8px;
            border-radius: 6px;
            border: none;
            outline: 1px solid rgba(0, 0, 0, 0.3);
        }
        input[type="text"]:focus {
            outline: 2px solid rgba(190, 98, 255, 0.7);
        }
        input[type="text"]:active {
            outline: 2px solid rgba(93, 41, 131, 0.7);
        }
    </style>
    {{ additional_content.head }}
</head>
<body>
    {{ additional_content.before_question }}
    <div class="question-holder">
        {% if hcaptcha.enabled %}
            <div class="question">
                <p class="question-title">{{ hcaptcha.title }}</p>
                <div class="h-captcha question-captcha" data-theme="dark" data-sitekey="{{ hcaptcha.sitekey }}"></div>
            </div>
            <div class="divider"></div>
        {% endif %}
        {% for question in question_list %}
            <div class="question" id="question-{{ loop.index }}">
                <p class="question-title">{{ question.title }}</p>
                <p class="question-description">{{ question.description | newline_to_br }}</p>
                <input class="question-input" type="text" name="question_1" placeholder="Answer" />
            </div>
            <div class="divider"></div>
        {% endfor %}
        <div class="question">
            <p class="question-title">{{ minecraft_name.title }}</p>
            <p class="question-description">{{ minecraft_name.description }}</p>
            <input class="question-input" type="text" name="question_minecraft_name" placeholder="Minecraft user name" />
        </div>
    </div>
    {{ additional_content.after_question }}
</body>
</html>
